<!DOCTYPE html>
<html lang="pt-br">
<head>
    <!-- Metadados -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Nova Postagem</title>

    <!-- Bootstrap CSS incluído individualmente -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

    <!-- Seu CSS personalizado (se houver) -->
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partial/header') %>

    <div class="container mt-4">
        <h1 class="mb-4">Criar Nova Postagem</h1>
        <form action="/community/<%= communityId %>/posts" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="title">Título</label>
                <input type="text" id="title" name="title" class="form-control" placeholder="Digite o título da postagem" required>
            </div>
            <div class="form-group">
                <label for="content">Conteúdo</label>
                <textarea id="content" name="content" class="form-control" rows="5" placeholder="Escreva o conteúdo da postagem" required></textarea>
            </div>
            <div class="form-group">
                <label for="type">Tipo de Postagem</label>
                <select id="type" name="type" class="form-control" required>
                    <option value="" disabled selected>Selecione o tipo</option>
                    <option value="pergunta">Pergunta</option>
                    <option value="enquete">Enquete</option>
                </select>
            </div>
            <div class="form-group">
                <label for="image">Imagem (opcional)</label>
                <input type="file" id="image" name="image" class="form-control-file">
            </div>
            <button type="submit" class="btn btn-primary mt-3">Criar Postagem</button>
        </form>
    </div>

    <!-- Bootstrap JS e dependências (se necessário) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
